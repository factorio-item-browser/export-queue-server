version: "3.5"
services:

  php:
    container_name: fib-eq-php
    image: bluepsyduck/php-apache
    volumes:
    - ".:/project"
    - "$SSH_AUTH_SOCK:/ssh-agent"
    networks:
    - network-fib
    ports:
    - 8040:80
    environment:
      FIB_ENV: development
      SSH_AUTH_SOCK: /ssh-agent

  mysql:
    container_name: fib-eq-mysql
    image: bluepsyduck/mysql
    networks:
    - network-fib
    ports:
    - 13346:3306

networks:
  network-fib:
    name: network-fib
